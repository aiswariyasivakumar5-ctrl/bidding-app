apiVersion: v1
kind: Service
metadata:
  name: bidding-service
spec:
  type: NodePort
  selector:
    app: bidding-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007